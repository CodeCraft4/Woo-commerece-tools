import { Typography } from "@mui/material";
import DashboardLayout from "../../../layout/DashboardLayout";

const Categories = () => {
  return (
    <DashboardLayout>
      <Typography sx={{ fontSize: { md: "35px", sm: "30px", xs: "auto" } }}>
        CATEGORIES
      </Typography>
    </DashboardLayout>
  );
};

export default Categories;
